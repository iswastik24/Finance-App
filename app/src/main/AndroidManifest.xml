<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.swastikijari.intellegent_finance_app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login_signUp"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Stocks" />
        <activity android:name=".Bonds" />
        <activity android:name=".Mutual_funds" />
        <activity android:name=".Commodity" />
        <activity android:name=".Real_estate" />
        <activity android:name=".Annuties" />
        <activity android:name=".Portfolio" />
        <activity android:name=".ChatBot" />
        <activity android:name=".MainActivity" />
        <activity android:name=".PieChartDisplay"></activity>
    </application>

</manifest>